NS = Object.create(null)
describe ('Tests: datamodel_model', function () {
  it ('Datamodel_model: basic', function () {
    chai.assert(false, 'NOT YET IMPLEMENTED')
  })
})

eYo.Debug.test() // remove this line when finished
