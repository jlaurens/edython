describe('Grammar', function() {
  var assert = chai.assert;

  describe('Creation', function() {
    it('test', function() {
      assert(eYo.gmr)
      var g = new eYo.gmr.Book()
      assert(g)
    })
  })
  
})